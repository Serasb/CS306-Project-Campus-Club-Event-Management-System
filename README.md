# CS-306-Campus-Club-Event-Management-System
Campus club event management system with support ticket module implemented using PHP and MySQL.

## Running the Project
This project requires a local PHP and MySQL environment.
1. Install XAMPP or MAMP
2. Import the database schema into MySQL
3. Place the PHP files inside the web server directory
4. Open the project in a browser using localhost
